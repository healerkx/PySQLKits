

db = @mysql('localhost', 'root', 'root');

user = db.test.kx_user(create_time < @today(-2));
@p(user);

s = db.ndcdb.user_station();
@p(s|user_id, station_id|); # print users

# here's the comment line
@p('----------------------------------------');
